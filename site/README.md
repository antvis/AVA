# ava site

Quick start:

```bash
cd site
npm install
npm run start
```

Notes:
- `@antv/ava` is referenced as a local file dependency to `../../src` (existing in repo). If you prefer to use the published package, update `package.json` accordingly.
- Some TS/IDE lint warnings may appear until dependencies are installed.

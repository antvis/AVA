/**
 * SaaS-hosted data engines: schema discovered and SQL executed through the
 * provider's API, data never materialized locally.
 */

export { SupabaseEngine, SupabaseApiError } from './supabase';

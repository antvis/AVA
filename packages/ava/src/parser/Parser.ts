export class Parser {
  parse() {
    const data = {};
    const purpose = {};

    if (true) {
      // 通过代码检验是否是可结构化解析的文本，抽样探测加 try catch 的格式化，如果是，直接解析为 data，并尽可能标记 shape
      // 调用 data 中的 infer 方法，支持 csv, json, jsonl 等格式
    } else {
      // 调用 agent 解析
    }

    // 返回结构化数据和基于数据的意图
    return { data, purpose };
  }
}

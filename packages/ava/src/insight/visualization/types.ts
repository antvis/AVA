import type { PatternInfo, PointPatternInfo, HomogeneousPatternInfo, InsightType, ChartType } from '../types';

interface Text {
  content: number | string;
  style?: Record<string, number | string>;
}



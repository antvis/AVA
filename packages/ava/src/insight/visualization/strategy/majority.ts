import { G2Spec } from '@antv/g2';

import { MajorityInfo, InsightInfo } from '../../types';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const majorityStrategy = (insight: InsightInfo<MajorityInfo>): G2Spec => {
  return {};
};

// FIXME

describe('', () => {
  test('', () => {
    console.log('fixme');
  });
});

// import { Linter } from '../../../src/linter';

// describe('UNIT TEST - chart-linter/linter/linter', () => {
//   const input = [
//     'mark(bar).',
//     'data("data/cars.json").',
//     'encoding(e0).',
//     'channel(e0,x).',
//     'field(e0,"Origin").',
//     'fieldtype(origin, string).',
//     'aggregate(e0,sum).',
//     'bin(e0,10).',
//     'type(e0,nominal).',
//     'encoding(e1).',
//     'channel(e1,y).',
//     'field(e1,"Horsepower").',
//     'fieldtype(horsepower, number).',
//     'extent(horsepower, 46, 230).',
//     'type(e1,quantitative).',
//     'aggregate(e1,sum).',
//     'zero(e1).',
//   ];

//   // const output = [
//   //   { id: 'aggregate_nominal', param1: 'x' },
//   //   { id: 'bin_and_aggregate', param1: 'x' },
//   //   { id: 'bin_q_o', param1: 'x', param2: 'nominal' },
//   // ];

//   test('should work', async () => {
//     // FIXME: need a real test case
//     const logger = (t: string) => {
//       console.log(t);
//     };
//     const linter = new Linter(logger);

//     linter.init().then(() => {
//       const solution = linter.solve(input.join('\n'), { models: 5 });
//       console.log(solution);
//     });
//   });
// });
